import React from 'react'
import {CartItems} from './Components/CartItems/CartItems' // Adjust the path if needed

export const Cart = () => {
  return (
    <div>
        <CartItems />
    </div>
  )
}
